[package]
name = "dns-resolver"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
rand = "0.9.2"
thiserror = "1.0"
gossip-membership = { path = "../gossip-membership" }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
