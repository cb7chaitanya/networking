[package]
name = "raft-consensus"
version = "0.1.0"
edition = "2021"
description = "Raft consensus algorithm implemented from scratch in Rust"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
